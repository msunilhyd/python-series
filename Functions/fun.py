# A function is a reusable code that can be called anywhere in a program
# Funtions improve readability of code  

# The def keyword tell Python that we have a function.

# The return value is the value the function returns
# Not all functions have to return something


def f(x):
    return x*x

print(f(3))